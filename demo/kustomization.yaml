apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - EgressFirewall.yaml

patches:
- target:
    kind: EgressFirewall
    name: default
  options:
    allowNameChange: true
  patch: |-
      - op: replace
        path: /metadata/name
        value: default
      - op: replace
        path: /metadata/labels/env
        value: replace


namespace: default
namePrefix: nobel-ms-
nameSuffix: -dev
